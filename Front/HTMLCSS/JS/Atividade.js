// while(true) = coleta de dados
// for (let === 1; i >= 0; 1++) = processamento de dados
// for letra of palavra = exibição de dados

alert('Saudações!')

contador = 0

while(true) {
    let nome = (prompt('Insira um nome:'))
    if (nome === 'sair') {
        alert('saindo...')
        break
    } else {
        for
    }
}